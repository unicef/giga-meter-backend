# Changelog

Append-only. One line per merged PR: `YYYY-MM-DD — PR-NNN — summary`.

2026-05-18 — PR-321 — Cloudflare upload via `POST /api/v1/measurements/:protocol`, protocol-config CRUD/resolve, measurements `protocol` + quality columns (ADR 001)
